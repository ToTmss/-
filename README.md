 Django Project — Учебный Проект

Этот репозиторий содержит простой учебный проект на Django, созданный специально для выполнения задания и освоения базовых навыков веб-разработки.

Проект демонстрирует:

* работу Django-приложения,
* маршрутизацию (urls),
* представления (views),
* шаблоны (templates),
* взаимодействие с базой данных SQLite,
* структуру Django-проекта.

---

## 📂 Структура проекта

```
project_name/
│
├── project_name/        # Основные настройки проекта
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
│
├── app_name/            # Основное Django-приложение
│   ├── models.py        # Модели (БД)
│   ├── views.py         # Логика отображений
│   ├── urls.py          # Локальные маршруты
│   ├── templates/       # HTML-шаблоны
│   └── forms.py         # Форма (если используется)
│
├── manage.py            # Основной управляющий скрипт
└── requirements.txt     # Зависимости проекта
```

---

## ⚙️ Установка и запуск проекта

### 1. Клонируйте репозиторий

```
git clone https://github.com/your-username/your-repo.git
```

### 2. Перейдите в директорию проекта

```
cd your-repo
```

### 3. Создайте виртуальное окружение

```
python -m venv venv
```

### 4. Активируйте окружение

Windows:

```
venv\Scripts\activate
```

Mac / Linux:

```
source venv/bin/activate
```

### 5. Установите зависимости

```
pip install -r requirements.txt
```

### 6. Примените миграции к базе данных

```
python manage.py migrate
```

### 7. Запустите сервер разработки

```
python manage.py runserver
```

После запуска проект будет доступен по адресу:
[http://127.0.0.1:8000/](http://127.0.0.1:8000/)


## 🗂 Используемые технологии

* **Python 3**
* **Django**
* **SQLite**
* HTML, CSS (внутри шаблонов)
